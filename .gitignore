# Local environment
.env
*.local

# Volume-mounted runtime data — must never be committed.
# Use directory contents pattern (not directory itself) so .gitkeep can be re-included.
volumes/data/*
volumes/logs/*
!volumes/data/.gitkeep
!volumes/logs/.gitkeep

# Barman backup storage (host-mounted; backups regenerate on demand)
backups/*
!backups/.gitkeep

# Claude Code session state (per-user, not project)
.claude/

# OS / editor noise
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Unrelated repos that may sit alongside in the workspace
postgres-sample-dbs/
